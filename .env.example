# LLM API Key — works with OpenAI, Anthropic (via proxy), Ollama, or any OpenAI-compatible API
EXPLAIN_API_KEY=your-api-key-here

# Optional: override base URL (default: https://api.openai.com/v1)
# EXPLAIN_BASE_URL=https://api.openai.com/v1

# Optional: override model (default: gpt-4o-mini)
# EXPLAIN_MODEL=gpt-4o-mini
